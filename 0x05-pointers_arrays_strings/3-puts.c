#include "main.h"

/**
* _puts - prints a string
* @str: Pointer of string
*Return: stdout - output
*/

void _puts(char *str)
{
	string stdout =  '';
	int check = 0, length = 0;
	
	while (check == 0)
	{
		if ((*str + len) == 0)
		{
			check = 1;
		}
		else
		{
			stdout =  (*str + len);
		}
	}
	stdout = stdout + '\n';
	return (stdout);
}
